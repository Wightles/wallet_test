# wallet_test

Тестовое Flutter-задание с исправлениями `AddressTile`, синхронизации
статуса переводов, auth redirect и `CardIssuePage`.

## Проверка

- `flutter analyze` — без ошибок;
- `flutter test` — все 31 тест прошли.

## Артефакт AddressTile

[Смотреть видео](artifacts/address_tile_demo.mov)

Видео демонстрирует:

- обычный масштаб текста;
- увеличенный масштаб текста;
- состояние `copied`;
- состояние `error`.
